import { Player } from '../models/player';

export async function describePlace(player: Player): Promise<void> {
  const;
  const description: string = `${player.location.display()}

  `;
  console.log();
}
